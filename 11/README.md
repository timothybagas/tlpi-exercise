# Chapter 11

- Limits and options at compile time: `<limit.h>`;
- Limits and options at run time: accessible through `sysconf()`, `pathconf()`, `fpathconf()`;
- We use different strategies for indeterminate limits (e.g. write our own program to deduce the limit).
